baseURL = 'https://rschmaelzle.github.io/studio/'
languageCode = 'en-us'
title = 'Studio Evergreen'
theme = 'blowfish'

enableRobotsTXT = true

[params]
  colorScheme = "blowfish" 
  
  # NUCLEAR FIX: Use the FULL URL for the background image
  defaultBackgroundImage = "https://rschmaelzle.github.io/studio/static/background.jpg"
  
  # NUCLEAR FIX: Use the FULL URL for the favicon
  favicon = "https://rschmaelzle.github.io/studio/static.favicon.svg"

  [params.homepage]
    layout = "hero" 
    showRecent = false 
    
  [params.header]
    layout = "basic"

  [params.list]
    showHero = false
    showSummary = true
    showTableOfContents = false
    showBreadcrumbs = true

[menu]
  # 1. THE LIBRARY
  [[menu.main]]
    identifier = "library"
    name = "📖 The Library"   # Icon is now part of the text
    url = "/library/"
    weight = 10

  # 2. THE WORKBENCH
  [[menu.main]]
    identifier = "workbench"
    name = "💻 The Workbench" # Icon is now part of the text
    url = "/workbench/"
    weight = 20

  # 3. GALLERIE IMMERGRÜN
  [[menu.main]]
    identifier = "immergruen"
    name = "👁️ Gallerie Immergrün" # Icon is now part of the text
    url = "/immergruen/"
    weight = 30

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true