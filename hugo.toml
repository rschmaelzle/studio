baseURL = 'https://rschmaelzle.github.io/studio/'
languageCode = 'en-us'
title = 'Studio Evergreen'
theme = 'blowfish'

enableRobotsTXT = true

[params]
  colorScheme = "blowfish" 
  
  # NUCLEAR FIX: Use the FULL URL for the background image
  defaultBackgroundImage = "https://rschmaelzle.github.io/studio/background.jpg"
  
  # NUCLEAR FIX: Use the FULL URL for the favicon
  favicon = "https://rschmaelzle.github.io/studio/favicon.svg"

  [params.homepage]
    layout = "hero" 
    showRecent = false 
    
  [params.header]
    layout = "basic"

  [params.list]
    showHero = false
    showSummary = true
    showTableOfContents = false
    showBreadcrumbs = true

[menu]
  # 1. THE LIBRARY
  [[menu.main]]
    identifier = "library"
    name = "The Library"
    url = "/library/"
    weight = 10
    pre = "📖"            # Emoji Fallback (Universal)
    params.icon = "book-open"

  # 2. THE WORKBENCH
  [[menu.main]]
    identifier = "workbench"
    name = "The Workbench"
    url = "/workbench/"
    weight = 20
    pre = "💻"            # Emoji Fallback
    params.icon = "terminal"

  # 3. GALLERIE IMMERGRÜN
  [[menu.main]]
    identifier = "immergruen"
    name = "Gallerie Immergrün"
    url = "/immergruen/"
    weight = 30
    pre = "👁️"            # Emoji Fallback
    params.icon = "eye"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true